/**
 * The same picture for X, which reads a different tag for the same job.
 * Kept as a re-export so there is one design, not two that drift.
 */
export { default, size, contentType, alt } from "./opengraph-image";
